project:
  name: "Research_FREnTE"
  region_focus: "São Paulo -> Três Lagoas"

pipeline:
  query: "impactos humanos no Rio Tietê"
  limit: 10
  dry_run: true

outputs:
  reports_dir: "reports/"
  data_dir: "data/"
