project:
  name: "Research_FREnTE"
  region_focus: "São Paulo -> Três Lagoas"

pipeline:
  query: "impactos humanos no Rio Tietê"
  limit: 10
  dry_run: true
  web_research_mode: "mock"  # mock | real
  web_timeout_seconds: 8

outputs:
  reports_dir: "reports/"
  data_dir: "data/"
